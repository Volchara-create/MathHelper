# Аудит проекту MathHelper
_Оновлено: 14.04.2026_

---

## 📌 Пріоритет розробки

- **Зараз**: тільки десктоп версія (ПК/ноутбук)
- **Мобільна версія**: буде пізніше, після десктопу
- Мобільні баги позначені як ⏸ MOBILE — не виправляти зараз

---

## 🔴 КРИТИЧНІ — виправити першочергово

*(Немає — всі критичні виправлено)*

---

## 🟡 СЕРЕДНІ — виправити найближчим часом

1. **`tasksInit()` — показує задачі 7 класу незалогіненим** (`script.js`)
   `getUserGrade()` повертає `null` → завантажуються задачі для 7 класу без попередження.

2. **Notes Drawer недоступний з UI** (`script.js`)
   `notesDrawerOpen()` існує, але жодна кнопка в інтерфейсі її не викликає.

3. **`buildGeoTab()` не оновлюється при зміні класу** (`script.js`)
   Guard `if (grid.children.length > 0) return` не дає оновитися після зміни класу.

4. **Quick menu не підсвічує активні панелі** (`script.js`)
   Відкрив калькулятор — кнопка не отримує клас `active`.

5. **Квіз і графік — немає кнопки для float-панелі** (`script.js`)
   `openFloatPanel('quiz')` існує, але жодна кнопка в UI її не викликає.
   Графік як float-панель — складно (canvas потребує фіксованих розмірів), відкладено.

---

## ⏸ MOBILE — виправити коли буде мобільна версія

- Header ламається на < 400px (`style.css`)
- Quick-menu перекриває контент на мобільних
- Workspace (граф) не працює на touch-екранах — тільки mouse events

---

## ⏸ Пізніше / Після оплати

- **Hero stats "∞ графіків"** — поки немає реального ліміту; виправити коли буде payment flow
- **Соцмережі TikTok/Instagram/Facebook** — ведуть на головні сторінки; виправити коли будуть акаунти проєкту

---

## ⏸ Відомі проблеми — не чіпати (технічний борг)

### Battle (Битва множення)
- `startBattle()` не викликає `show()` → кнопка ← не з'являється
- Жодна кнопка в UI не веде до `#battle` (orphaned feature)
- **Рішення**: залишити як є, не показувати. Виправити навігацію коли готово до релізу.

### Безпека
- **`confirmGoogleGrade()` — XSS-ризик в onclick** (`auth.js`) — JWT в HTML атрибуті
- **Service Worker не очищує старий кеш** (`sw.js`) — немає версіонування

---

## ✅ Вже виправлено

### Сесія 14.04.2026 — Критичні баги
- Таймер НМТ не зупинявся після "Здати"
- GitHub Pages API URL
- НМТ 404 на GitHub Pages (відносні шляхи)
- Обидва екрани НМТ показувались одночасно
- AdSense placeholder
- `isPro` не повертався з сервера
- Settings modal замість `prompt()`
- Кнопка ← Головна в хедері
- Клас і щоденна ціль після перезавантаження
- Push нагадування о 18:00
- Щоденна ціль з прогрес-баром
- XSS в нотатках — `escHtml()` для title/content
- `noteDelete` без перевірки сервера
- `notesDrawerOpen` без res.ok
- `tasksInit` скидав режим

### Сесія 14.04.2026 — UI/UX
- **Два зошити** — видалено лівий `panel-notebook` (залишився плаваючий)
- **`ws-notebook` і `ws-calc` в графіку** — видалено, в графіку тільки `ws-props`
- **НМТ дублювання** — видалено `dash-nmt-section` і `hero-nmt-banner`
- **`updateNavForGrade` crash** — видалено виклики неіснуючої функції
- **Home кнопка для незалогінених** — тепер веде на `home`
- **Дублікат `push-toggle-btn`** — другій кнопці `id="push-toggle-btn-settings"`, `updatePushBtn` оновлює обидві
- **Мертвий `#category-row`** — видалено з HTML
- **`nbSaveAsNote` API URL** — спрощено до `${API}/notes`
- **`startTrial()` alert** — замінено на `showToast()`, додано toast CSS + JS
- **Loading-стан нотаток** — додано "Завантаження..." перед fetch в `dashLoadRecentNotes`
- **`updateNavForGrade` в auth.js** — видалено залишок у `authShowUser`
- **`buildGeoTab()` grade guard** — виправлено раніше (`dataset.builtForGrade`)
- **Калькулятор display** — вираз показується у великому полі, після `=` результат великий + вираз малий
- **NMT симулятор** — кнопка `🏠 Головна` → два прямокутники `←` і `🏠 Головна` по центру

### Сесія 14.04.2026 — Нові фічі
- **Плаваючі панелі** — зошит і калькулятор як `side-panel` (перетягування, зміна розміру)
- **Snap zones** — при перетягуванні float-панелей з'являються 4 зони по кутах екрана; наблизив → автоснеп
- **Селектор конспектів в зошиті** — вибір збережених нотаток, збереження на сервер
- **Деталі формул** — кнопка "Детальніше" в алгебрі і тригонометрії відкриває модалку з поясненням, доведенням і прикладом (~35 формул)
- **Float quiz панель** — HTML і JS структура додана (`float-quiz`), кнопка відкриття — TODO
- **CI/CD автодеплой** — GitHub Actions деплоїть на VPS після кожного `git push` до `main`

### Сесія 14.04.2026 — Виправлення drag/scroll
- **Скрол панелей** — `side-panel-body` змінено на `overflow-y:auto`, `sp-nb-body` отримав `min-height:0`
- **Перетягування вліво/вправо** — виправлена логіка drag (раніше тільки вліво), тепер в обидві сторони + ghost placeholder
