# Аудит проекту MathHelper
_Оновлено: 15.04.2026_

---

## 📌 Пріоритет розробки

- **Зараз**: тільки десктоп версія (ПК/ноутбук)
- **Мобільна версія**: буде пізніше, після десктопу
- Мобільні баги позначені як ⏸ MOBILE — не виправляти зараз

---

## 🔴 КРИТИЧНІ — виправити першочергово

*(Немає — всі критичні виправлено)*

---

## 🟡 СЕРЕДНІ — виправити найближчим часом

1. **Notes Drawer недоступний з UI** (`auth.js`)
   `notesDrawerOpen()` існує в `auth.js`, але жодна кнопка в інтерфейсі її не викликає.

2. **Квіз float-панель — немає кнопки відкриття** (`script.js`)
   `openFloatQuiz()` і HTML `#float-quiz` існують, але жодна кнопка в UI їх не викликає.
   Графік як float-панель — складно (canvas потребує фіксованих розмірів), відкладено.

3. **НМТ симулятор — стиль не відповідає основному сайту** (`simulator.html`)
   Лише косметична проблема, не критично. Виправити окремо.

4. **Push-нагадування не відображаються як сповіщення** (`sw.js`, `auth.js`)
   Немає візуальної перевірки. Потребує тестування в браузері з дозволом на сповіщення.
   ⏸ Відкладено: складно відлагодити без реального пристрою.

---

## ⏸ MOBILE — виправити коли буде мобільна версія

- Header ламається на < 400px (`style.css`)
- Quick-menu перекриває контент на мобільних
- Workspace (граф) не працює на touch-екранах — тільки mouse events

---

## 📐 Специфікація футера (НЕ ЗМІНЮВАТИ без потреби)

Футер знаходиться **всередині `<main>`** (скролиться з контентом), перед `</main>`.

**HTML**: `<footer>` — звичайний тег, без ID, з класом `.socials` для іконок.
**CSS**: `footer{text-align:center; padding:14px 16px 68px; background:var(--blue); color:#fff; border-top:2px solid var(--accent); margin-left:-24px; margin-right:-24px; margin-bottom:-30px;}`

- `margin:-24px` з боків — компенсує padding main (24px), прибирає білі краї
- `margin-bottom:-30px` — прибирає білий простір після футера
- `padding-bottom:68px` — простір щоб quick-menu не перекривав контент
- При open quick-menu: `footer{margin-bottom:-72px}` (бо main отримує padding-bottom:72px)
- Іконки flaticon БЕЗ filter — кольорові на синьому фоні
- На графіку footer прихований: `:has(section#graph.active) footer{display:none}`

---

## 📋 Специфікація модалу формул (НЕ ЗМІНЮВАТИ без потреби)

Єдина система для всіх формул: алгебра, тригонометрія, геометрія — клік → `#formula-detail-modal`.

**JS функція**: `openFormulaModal({name, expr, detail, svg, symbols})`
- `name` — назва формули
- `expr` — сам вираз (показується великим жирним)
- `detail` — об'єкт `{explanation, proof, example}` — необов'язково
- `svg` — SVG розмітка (для геометрії)
- `symbols` — масив `[{s, m}]` таблиця символів (для геометрії)

**Точки виклику:**
- Алгебра/Тригонометрія: `_buildAlgRow()` → `openFormulaModalByIdx(catIdx, fIdx, source)` → `openFormulaModal()`
- Геометрія: `showCategory()` → `d.onclick = () => openFormulaModal({...})` — без проміжного кроку
- Кнопки "Детальніше" — **видалені**, замість них клік на весь рядок

**CSS ключові стилі:**
- `.fdm-formula` — `font-size:1.7rem; font-weight:700; gradient background; word-break:break-word`
- `.alg-modal-expr` — `white-space:nowrap; text-overflow:ellipsis` (не переноситься в рядку)
- `.alg-modal-row` — `cursor:pointer; hover: translateX(3px)` (підказка що клікабельно)
- `.fdm-content` — `max-width:680px; max-height:88vh`

**Якщо немає detail/svg/symbols** → показується `.fdm-no-detail` повідомлення (НЕ ЛАМАЄТЬСЯ).

---

## ⏸ Пізніше / Після оплати

- **Hero stats "∞ графіків"** — поки немає реального ліміту; виправити коли буде payment flow
- **Соцмережі TikTok/Instagram/Facebook** — ведуть на головні сторінки; виправити коли будуть акаунти проєкту

---

## 💡 Ідеї для майбутнього (Pro версія)

### SmartOwl (Mathik Pro з ШІ)
- **Концепція**: при покупці Pro в quick-menu кнопка 🤖 ШІ змінюється на 🦉⚡ — "SmartOwl"
- **Анімація**: звичайна сова стає "металевою" / з ефектом glow при активації
- **Функція**: замість keyword-бота — реальний AI (Claude API) для пояснення математики
- **Назви**: SmartOwl / МудрикPro / Сова3000 / OWL-AI
- **Технічно**: Claude API via backend, ~$0.001/запит, тільки для Pro users
- **Важливо**: звичайний Mathik (🦉) залишається для всіх безкоштовно

---

## ⏸ Відомі проблеми — не чіпати (технічний борг)

### Battle (Битва множення)
- `startBattle()` не викликає `show()` → кнопка ← не з'являється
- Жодна кнопка в UI не веде до `#battle` (orphaned feature)
- **Рішення**: залишити як є, не показувати. Виправити навігацію коли готово до релізу.

### Безпека
- **`confirmGoogleGrade()` — XSS-ризик в onclick** (`auth.js`) — JWT в HTML атрибуті
- **Service Worker не очищує старий кеш** (`sw.js`) — немає версіонування

---

## 🦉 Специфікація Mathik-огляду (НЕ ЗМІНЮВАТИ структуру без потреби)

Константа `MATHIK_TUTORIAL` в `script.js` — масив кроків повного огляду сайту.

### Типи кроків

**Звичайний крок** (повідомлення + "▶ Далі"):
```js
{ target: '#selector', msg: 'HTML-текст', navigate: () => { /*тиха навігація*/ } }
```
- `target` — CSS-селектор (кома = кілька варіантів, перший видимий)
- `msg` — HTML-текст у бульці (підтримує `<b>`, `<kbd>`)
- `navigate` *(опц.)* — тиха дія ДО польоту (show, openPanel тощо)

**autoClick крок** (сова летить → пульс 1.5с → action() → наступний крок сам):
```js
{ autoClick: true, target: '#btn', clickMsg: 'текст під час пульсу', action: () => { /*клік*/ }, navigate: () => { /*опц.*/ } }
```

**Фінальний крок** (сова повертається додому → текст 5с → зникає):
```js
{ isLast: true, msg: '🎉 ...' }
```

### Поточна структура (15 кроків)

| # | Розділ | Тип |
|---|--------|-----|
| 0 | Вступ | звичайний (без target) |
| 1 | Натискає кнопку Формули на дашборді | autoClick |
| 2 | Вкладка Алгебра (пояснення розділу) | звичайний |
| 3 | Рядок формули (як відкрити детально) | звичайний + navigate |
| 4 | Натискає кнопку Квіз на дашборді | autoClick + navigate |
| 5 | Кнопка теми квізу (пояснення) | звичайний |
| 6 | Натискає кнопку Графіки на дашборді | autoClick + navigate |
| 7 | Поле вводу функції (пояснення) | звичайний |
| 8 | НМТ кнопка в quick-menu | звичайний + navigate |
| 9 | Зошит (відкриває openPanel) | звичайний + navigate |
| 10 | Калькулятор (відкриває openPanel) | звичайний + navigate |
| 11 | Швидке меню (після закриття панелі) | звичайний + navigate |
| 12 | Кнопка темної теми `#dark-toggle` | звичайний |
| 13 | Кнопка пошуку `#search-btn` | звичайний |
| 14 | Кнопка налаштувань | звичайний |
| 15 | Фінал — сова летить додому | isLast |

### Правило: додавання нової функції
> **Коли додаєш нову фічу** — одразу додай крок у `MATHIK_TUTORIAL` (перед фіналом, крок 15).
> Описи: що це, як користуватися, навіщо. Формат — звичайний або autoClick.

---

## ✅ Вже виправлено

### Сесія 14.04.2026 — Критичні баги
- Таймер НМТ не зупинявся після "Здати"
- GitHub Pages API URL
- НМТ 404 на GitHub Pages (відносні шляхи)
- Обидва екрани НМТ показувались одночасно
- AdSense placeholder
- `isPro` не повертався з сервера
- Settings modal замість `prompt()`
- Кнопка ← Головна в хедері
- Клас і щоденна ціль після перезавантаження
- Push нагадування о 18:00
- Щоденна ціль з прогрес-баром
- XSS в нотатках — `escHtml()` для title/content
- `noteDelete` без перевірки сервера
- `notesDrawerOpen` без res.ok
- `tasksInit` скидав режим

### Сесія 14.04.2026 — UI/UX
- **Два зошити** — видалено лівий `panel-notebook` (залишився плаваючий)
- **`ws-notebook` і `ws-calc` в графіку** — видалено, в графіку тільки `ws-props`
- **НМТ дублювання** — видалено `dash-nmt-section` і `hero-nmt-banner`
- **`updateNavForGrade` crash** — видалено виклики неіснуючої функції
- **Home кнопка для незалогінених** — тепер веде на `home`
- **Дублікат `push-toggle-btn`** — другій кнопці `id="push-toggle-btn-settings"`, `updatePushBtn` оновлює обидві
- **Мертвий `#category-row`** — видалено з HTML
- **`nbSaveAsNote` API URL** — спрощено до `${API}/notes`
- **`startTrial()` alert** — замінено на `showToast()`, додано toast CSS + JS
- **Loading-стан нотаток** — додано "Завантаження..." перед fetch в `dashLoadRecentNotes`
- **`updateNavForGrade` в auth.js** — видалено залишок у `authShowUser`
- **`buildGeoTab()` grade guard** — виправлено раніше (`dataset.builtForGrade`)
- **Калькулятор display** — вираз показується у великому полі, після `=` результат великий + вираз малий
- **NMT симулятор** — кнопка `🏠 Головна` → два прямокутники `←` і `🏠 Головна` по центру

### Сесія 14.04.2026 — Нові фічі
- **Плаваючі панелі** — зошит і калькулятор як `side-panel` (перетягування, зміна розміру)
- **Snap zones** — при перетягуванні float-панелей з'являються 4 зони по кутах екрана; наблизив → автоснеп
- **Селектор конспектів в зошиті** — вибір збережених нотаток, збереження на сервер
- **Деталі формул** — кнопка "Детальніше" в алгебрі і тригонометрії відкриває модалку з поясненням, доведенням і прикладом (~35 формул)
- **Float quiz панель** — HTML і JS структура додана (`float-quiz`), кнопка відкриття — TODO
- **CI/CD автодеплой** — GitHub Actions деплоїть на VPS після кожного `git push` до `main`

### Сесія 14.04.2026 — Виправлення drag/scroll
- **Скрол панелей** — `side-panel-body` змінено на `overflow-y:auto`, `sp-nb-body` отримав `min-height:0`
- **Перетягування вліво/вправо** — виправлена логіка drag (раніше тільки вліво), тепер в обидві сторони + ghost placeholder

### Сесія 15.04.2026 — Аудит і виправлення (частина 1)
- **Quick menu активна підсвітка** — `openPanel`/`closePanel` тепер викликають `_updateQmBtnActive()`, CSS додано `.qm-btn.active`
- **Квіз скидається при поверненні** — `startQuiz()` тепер відновлює квіз якщо він в процесі

### Сесія 15.04.2026 — Виправлення (частина 2)
- **Темний режим зламаний** — функція `toggleDark()` зникла під час рефакторингу; відновлено в script.js, зберігає `mh_dark` в localStorage
- **Панелі не тримаються при скролі** — `body/html` змінено з `min-height:100%` на `height:100%; overflow:hidden`; `<main>` отримав `min-height:0` — тепер тільки `<main>` скролиться, а не документ
- **Drag калькулятора** — виправлено на `getBoundingClientRect()` + clamping до меж viewport
- **Підсвітка quick menu** — тепер яскравіша: темний фон + акцентний колір + верхній бордер
- **Граф автомасштаб** — `openPanel`/`closePanel` тепер викликають `_resizeGraphAfterPanel()` з затримкою 320ms (після CSS transition)
